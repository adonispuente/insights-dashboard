apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: cert-manager-allow-inbound-webhook
spec:
  ingress:
  - ports:
    - port: 10250
  podSelector:
    matchLabels:
      app: webhook
  policyTypes:
  - Ingress