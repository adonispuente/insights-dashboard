---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: entitlements
  labels:
    prometheus: app-sre
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: public
  namespaceSelector:
    matchNames:
    - entitlements-stage
  selector:
    matchLabels:
      app: entitlements-api-go
