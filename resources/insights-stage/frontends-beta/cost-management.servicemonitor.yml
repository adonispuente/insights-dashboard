---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cost-management-frontends-beta
  labels:
    prometheus: app-sre
spec:
  endpoints:
    - interval: 15s
      path: /metrics
      port: metrics
  namespaceSelector:
    matchNames:
      - frontends-beta
  selector:
    matchLabels:
      frontend: cost-management
