apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-from-internet
  namespace: backplane
spec:
  ingress:
  - from:
    - ipBlock:
        cidr: 0.0.0.0/0
  podSelector: {}
  policyTypes:
  - Ingress
