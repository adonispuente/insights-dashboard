apiVersion: cloud.redhat.com/v1alpha1
kind: FrontendEnvironment
metadata:
  name: {{env_name}}
spec:
  hostname: {{env_name}}-{{rando}}.{{hostname}}
  ingressClass: openshift-default
  sso: {{sso_source}}
  ssl: {{ssl}}
  enableAkamaiCacheBust: false
