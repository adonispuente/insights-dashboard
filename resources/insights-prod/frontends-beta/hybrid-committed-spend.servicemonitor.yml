---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: hybrid-committed-spend-frontends-beta
  labels:
    prometheus: app-sre
spec:
  endpoints:
    - interval: 15s
      path: /metrics
      port: metrics
  namespaceSelector:
    matchNames:
      - frontends-beta
  selector:
    matchLabels:
      frontend: hybrid-committed-spend
