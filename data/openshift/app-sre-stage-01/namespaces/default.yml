---
$schema: /openshift/namespace-1.yml

labels: {}

name: default

cluster:
  $ref: /openshift/app-sre-stage-01/cluster.yml

app:
  $ref: /services/app-sre/app.yml

environment:
  $ref: /products/app-sre/environments/stage.yml

description: Default namespace. It needs to be whitelisted for traffic in the dynatrace namespace for Dynatrace Webhooks to work.
