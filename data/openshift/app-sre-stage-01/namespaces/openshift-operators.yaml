---
$schema: /openshift/namespace-1.yml

labels: {}

name: openshift-operators
description: app-sre-stage-01 openshift-operators namespace

cluster:
  $ref: /openshift/app-sre-stage-01/cluster.yml

app:
  $ref: /services/app-sre/app.yml

environment:
  $ref: /products/app-sre/environments/stage.yml

managedRoles: true

managedResourceTypes:
- Subscription.operators.coreos.com

openshiftResources:
- provider: resource-template
  type: jinja2
  path: /tekton/openshift-pipelines-operator-rh.subscription.j2
- provider: resource
  path: /setup/network-observability/netobserv-operator.stable.subscription.yaml
- provider: resource
  path: /setup/advanced-cluster-security/rhacs-operator.subscription.yaml
- provider: resource
  path: /setup/tempo/tempo-operator.subscription.yaml
- provider: resource
  path: /setup/otel/otel-operator.subscription.yaml
