---
$schema: /app-interface/change-type-1.yml

labels: {}

name: elasticache-defaults-maintainer
description: |
  Allow updates on AWS ElastiCache defaults file in resources.

priority: high

contextType: resourcefile

changes:
- provider: jsonPath
  jsonPathSelectors:
  - deletion_protection
  - maintenance_window
  - parameter_group_name
  # re-enable after APPSRE-7864
  #- engine_version
