---
$schema: /app-interface/change-type-1.yml

labels: {}

name: jenkins-config-owner
description: |
  Allow updates on resource files referenced in jenkins config_path.

priority: low

contextType: datafile
contextSchema: /dependencies/jenkins-config-1.yml

changes:
- provider: change-type
  changeTypes:
  - $ref: /app-interface/changetype/resource-owner.yml
  context:
    where: backrefs
    selector: config_path
