---
$schema: /app-interface/change-type-1.yml

labels: {}

name: cluster-owner
description: |
  Allow all actions in namespaces of a cluster.

priority: high

contextType: datafile
contextSchema: /openshift/cluster-1.yml

changes:
- provider: change-type
  changeTypes:
  - $ref: /app-interface/changetype/cluster-owner-namespace-admin.yml
  context:
    selector: cluster.'$ref'
- provider: change-type
  changeTypes:
  - $ref: /app-interface/changetype/cluster-owner-namespace-admin.yml
  context:
    selector: cluster.'$ref'
    when: added
