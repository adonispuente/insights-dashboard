---
$schema: /app-sre/saas-file-2.yml

labels: {}

name: backstage
description: Components for backstage

app:
  $ref: /services/backstage/app.yml

pipelinesProvider:
  $ref: /services/insights/pipelines/tekton.crc-pipelines.appsrep05ue1.yaml

slack:
  workspace:
    $ref: /dependencies/slack/redhat-internal.yml
  channel: team-clouddot-info

managedResourceTypes:
- ServiceAccount
- Service
- Deployment

imagePatterns:
- quay.io/janus-idp/backstage-showcase 

resourceTemplates:
- name: backstage
  url: https://gitlab.cee.redhat.com/service/backstage
  path: /template.yml
  targets:
  - namespace:
      $ref: /services/backstage/namespaces/backstage-stage.yml
    ref: main
    parameters:
      IMGAE: quay.io/janus-idp/backstage-showcase 
      IMAGE_TAG: latest
