---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: dynatrace

name: ci-int-dynatrace-jobs

description: ''

app:
  $ref: /services/osd-operators/dynatrace/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: dynatrace-config
    label: osd-operators
    node: osd-operators
    gl_group: service
    gl_project: dynatrace-config
    quay_org: app-sre
    jobs:
    - 'gl-pr-check':
        display_name: dynatrace-config MR check
        pr_check_script_path: './hack/pr_check.sh'
