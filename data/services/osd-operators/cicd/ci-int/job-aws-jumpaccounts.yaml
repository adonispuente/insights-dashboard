---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-osd-operators-jumpaccounts-terraform-jobs

description: 'AWS Jump Accounts setup with Terraform'

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: aws-jumpaccounts
    label: osd-operators
    node: osd-operators
    gl_group: service
    gl_project: aws-jumpaccounts-terraform
    jobs:
      - 'gl-pr-check-aws-jumpaccounts-terraform':
          display_name: aws-jumpaccounts-terraform pr-check
