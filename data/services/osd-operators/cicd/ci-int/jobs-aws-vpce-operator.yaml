---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-osd-operators-aws-vpce-operator-jobs

description: ''

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: aws-vpce-operator
    label: osd-operators
    node: osd-operators
    gh_org: openshift
    gh_repo: aws-vpce-operator
    quay_org: app-sre
    build_deploy_script_path: make build-push && make e2e-image-build-push
    jobs:
    - 'gh-build-master':
        display_name: aws-vpce-operator build main
        branch: main
