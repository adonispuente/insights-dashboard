---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: ci-int-osd-operators-configure-goalert-operator-jobs

description: ''

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: configure-goalert-operator
    label: osd-operators
    node: osd-operators
    gh_org: openshift
    gh_repo: configure-goalert-operator
    quay_org: app-sre
    build_deploy_script_path: make build-push
    jobs:
    - 'gh-build-master':
        display_name: configure-goalert-operator build master
        branch: main
