---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: osd-operators

name: hypershift-dataplane-metrics-forwarder

description: 'Proxy for forwarding dataplane metrics to RHOBS'

app:
  $ref: /services/osd-operators/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: hypershift-dataplane-metrics-forwarder
    label: osd-operators
    node: osd-operators
    gh_org: openshift
    gh_repo: hypershift-dataplane-metrics-forwarder
    quay_org: app-sre
    build_deploy_script_path: './build/build_deploy.sh'
    jobs:
    - 'gh-build-master':
        display_name: hypershift-dataplane-metrics-forwarder build main
        branch: main
