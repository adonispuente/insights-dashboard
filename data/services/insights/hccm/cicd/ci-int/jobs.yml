---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: insights

name: ci-int-hccm-jobs

description: ''

app:
  $ref: /services/insights/hccm/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: hccm
    label: insights
    node: insights
    gh_org: project-koku
    gh_repo: koku
    quay_org: cloudservices
    jobs:
    - 'insights-hccm-s3-sync':
        display_name: 'insights-hccm-s3-sync'
        cron_expression: '0 23 * * *'

- project:
    name: s3-data-sync
    label: insights
    node: insights
    quay_org: cloudservices
    gl_group: cost-management
    gl_project: s3-data-sync
    jobs:
    - gl-build-master:
        display_name: s3-data-sync build-main
        branch: main
