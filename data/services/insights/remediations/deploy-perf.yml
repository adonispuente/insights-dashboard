---
$schema: /app-sre/saas-file-2.yml

labels:
  service: remediations
  platform: insights

name: remediations-perf
description: remediations app on the Insights Platform

app:
  $ref: /services/insights/remediations/app.yml

pipelinesProvider:
  $ref: /services/insights/pipelines/tekton.crc-pipelines.appsrep05ue1.yaml

slack:
  workspace:
    $ref: /dependencies/slack/redhat-internal.yml
  channel: proj-perf-insights-cluster

managedResourceTypes:
- ClowdApp
- ConfigMap
- CronJob
- Job

imagePatterns:
- quay.io/cloudservices

authentication:
  image:
    path: insights/quay/cloudservices-push
    field: all
    version: 3
  code:
    path: app-sre/creds/github-app-sre-bot
    field: openshift-saas-deploy
resourceTemplates:
- name: remediations-consumer
  path: /deployment/remediations-consumer-clowdapp.yaml
  url: https://github.com/RedHatInsights/remediations-consumer
  parameters:
    KAFKA_CONNECTION_TIMEOUT: 10000
    DB_SSL_ENABLED: true
    LOG_CW_ENABLED: true
    LOG_LEVEL: info
    CLEANER_SUSPEND: 'false'
  targets:
  - namespace:
      $ref: /services/insights/remediations/namespaces/perf-remediations-perf.yml
    ref: master
    parameters:
      NUM_REPLICAS: 3
      ENV_NAME: perf

- name: remediations-update-consumer
  path: /deployment/remediations-update-consumer-clowdapp.yaml
  url: https://github.com/RedHatInsights/remediations-consumer
  parameters:
    KAFKA_CONNECTION_TIMEOUT: 10000
    DB_SSL_ENABLED: true
    LOG_CW_ENABLED: true
    LOG_LEVEL: info
    KAFKA_CONSUMER_GROUP: remediations-update-consumer
  targets:
  - namespace:
      $ref: /services/insights/remediations/namespaces/perf-remediations-perf.yml
    ref: master
    parameters:
      NUM_REPLICAS: 3
      ENV_NAME: perf

- name: remediations
  path: /deployment/clowdapp.yml
  url: https://github.com/RedHatInsights/insights-remediations
  parameters:
    ADVISOR_IMPL: impl
    COMPLIANCE_IMPL: impl
    DB_SSL_ENABLED: true
    IMAGE_CHANGE_TRIGGER: true
    INVENTORY_IMPL: impl
    LOG_CW_ENABLED: true
    LOG_LEVEL: info
    MAX_REPLICAS: 5
    MIGRATION_COMMAND: npm run db:create; npm run db:migrate
    PATCHMAN_IMPL: impl
    PATH_PREFIX: /api
    RBAC_IMPL: impl
    RECEPTOR_IMPL: impl
    REMEDIATIONS_REPLICAS: 2
    SOURCES_IMPL: impl
    SSG_IMPL: impl
    SSG_HOST: http://playbooks-ssg:8080
    STRATEGY: Rolling
    TARGET_CPU_UTILIZATION: 80
    USERS_IMPL: impl
    VULNERABILITIES_IMPL: impl
  targets:
  - namespace:
      $ref: /services/insights/remediations/namespaces/perf-remediations-perf.yml
    ref:  f41197f570300efb3ad50fe6e306f76390697f84
    parameters:
      PLATFORM_HOSTNAME_URL: "cert.${PLATFORM_HOSTNAME}"
      MIN_REPLICAS: 2
      FLOORIST_SUSPEND: 'true'
      FLOORIST_IMAGE_TAG: 8d12513 # 2023-04-18
      FLOORIST_BUCKET_SECRET_NAME: insights-metrics-export-stage
      FLOORIST_SCHEDULE: '11 10 * * *'
