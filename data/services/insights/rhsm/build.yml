---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: rhsm

name: ci-ext-rhsm-jobs
description: rhsm ci-ext jobs

app:
  $ref: /services/insights/rhsm/app.yml

instance:
  $ref: /dependencies/ci-ext/ci-ext.yml

type: jobs

config:
- project:
    name: curiosity-frontend
    label: insights
    node: insights
    gh_org: RedHatInsights
    gh_repo: curiosity-frontend
    quay_org: cloudservices
    jobs:
    - "insights-gh-pipeline":
        display_name: 'curiosity-frontend pipeline'
        status_context: ci.ext.devshift.net PR pipeline
        branch: main
