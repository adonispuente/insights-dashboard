---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: starter-apps

name: ci-int-devprod-jobs
description: 'Jobs for DevProd Apps'

app:
  $ref: /services/insights/devprod/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: jobs

config:
- project:
    name: blackbox-monitor
    label: insights
    node: insights
    gl_group: insights-platform
    gl_project: blackbox-monitor
    quay_org: cloudservices
    jobs:
    - "insights-gl-pr-check":
        display_name: blackbox-monitor pr-check
    - "insights-gl-build-master":
        display_name: blackbox-monitor build-master
