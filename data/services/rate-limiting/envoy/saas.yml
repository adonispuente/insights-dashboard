---
$schema: /app-sre/saas-file-2.yml

labels:
  service: envoy

name: envoy
description: Envoy deployment used for testing rate-limiting/limitador service. This service should not be used in production

app:
  $ref: /services/rate-limiting/app.yml

pipelinesProvider:
  $ref: /services/app-interface/pipelines/tekton.app-sre-pipelines.appsrep05ue1.yaml

slack:
  workspace:
    $ref: /dependencies/slack/redhat-internal.yml
  channel: sd-app-sre-info

authentication:
  image:
    path: app-sre/quay/app-sre-pull
    field: all

managedResourceTypes:
- Deployment
- Service
- ConfigMap

imagePatterns:
- envoyproxy/envoy

resourceTemplates:
- name: envoy
  url: https://gitlab.cee.redhat.com/service/rate-limiting-templates
  path: /envoy-template.yaml
  targets:
  - namespace:
      $ref: /openshift/app-sre-stage-01/namespaces/app-sre-rate-limiting-test.yml
    ref: master
