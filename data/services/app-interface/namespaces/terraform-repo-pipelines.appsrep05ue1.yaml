---
$schema: /openshift/namespace-1.yml

labels:
  provider: tekton

name: terraform-repo-pipelines
description: terraform repo pipelines namespace

cluster:
  $ref: /openshift/appsrep05ue1/cluster.yml

app:
  $ref: /services/app-sre/app.yml

environment:
  $ref: /products/app-interface/environments/production.yml

managedRoles: true

managedResourceTypes:
  - Secret

sharedResources:
  - $ref: /services/app-interface/shared-resources/app-sre-pipelines.yml
