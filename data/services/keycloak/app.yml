---
$schema: /app-sre/app-1.yml

labels:
  service: keycloak

name: keycloak

description: |
  Auth/SSO for FedRAMP

architectureDocument: https://gitlab.cee.redhat.com/service/app-interface/-/raw/master/docs/keycloak/architecture/architecture.md

sopsUrl: https://gitlab.cee.redhat.com/TODO

grafanaUrls:
- title: TODOREPLACE
  url: https://TODO

escalationPolicy:
  $ref: /teams/swat/escalation-policies/general-policy.yml

dependencies:
- $ref: /dependencies/ci-int/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/gitlab/service.yml

onboardingStatus: InProgress

serviceOwners:
- name: Ecosystem AppEng
  email: ecosystem-appeng@redhat.com

codeComponents:
- name: keycloak
  resource: upstream
  url: https://gitlab.cee.redhat.com/service/keycloak
  gitlabRepoOwners:
    enabled: true
  gitlabHousekeeping:
    enabled: true
    rebase: true
  gitlabSync:
    sourceProject:
      name: keycloak
      group: service
      branch: main
    destinationProject:
      name: keycloak
      group: service
      branch: main
- name: keycloak-user-service
  resource: upstream
  url: https://gitlab.cee.redhat.com/service/keycloak-user-service
  gitlabRepoOwners:
    enabled: true
  gitlabHousekeeping:
    enabled: true
    rebase: true
  gitlabSync:
    sourceProject:
      name: keycloak-user-service
      group: service
      branch: main
    destinationProject:
      name: keycloak-user-service
      group: service
      branch: main
- name: keycloak-config-manager
  resource: upstream
  url: https://gitlab.cee.redhat.com/service/keycloak-config-manager
  gitlabRepoOwners:
    enabled: true
  gitlabHousekeeping:
    enabled: true
    rebase: true
- name: keycloak-interface
  resource: other
  url: https://gitlab.cee.redhat.com/service/keycloak-interface
  gitlabRepoOwners:
    enabled: true
  gitlabHousekeeping:
    enabled: true
    rebase: true
quayRepos:
- org:
    $ref: /dependencies/quay/app-sre.yml
  teams:
  - permissions:
    - $ref: /dependencies/quay/permissions/quay-membership-swat.yml
    role: read
  items:
  - name: keycloak
    description: Keycloak
    public: false
  - name: keycloak-user-service
    description: keycloak user service
    public: false
  - name: keycloak-config-manager
    description: keycloak config manager
    public: false
