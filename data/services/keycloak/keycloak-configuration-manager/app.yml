---
$schema: /app-sre/app-1.yml

labels:
  service: keycloak-configuration-manager
  platform: keycloak

name: keycloak-configuration-manager

description: |
  The keycloak-configuration-manager implements GitOps and reconciliation to  maintain a expected configuration on a target keycloak server instance.

architectureDocument: https://drive.google.com/file/d/1cFD7ViJtpwWSGDmRUK8cuPRnJF8TTQO8/view?usp=sharing

sopsUrl: https://gitlab.cee.redhat.com/TODO

grafanaUrls:
- title: TODOREPLACE
  url: https://TODO

parentApp:
  $ref: /services/keycloak/app.yml

escalationPolicy:
  $ref: /teams/swat/escalation-policies/general-policy.yml

dependencies:
- $ref: /dependencies/ci-int/service.yml
- $ref: /dependencies/quay/service.yml
- $ref: /dependencies/gitlab/service.yml

onboardingStatus: InProgress

serviceOwners:
- name: Ecosystem AppEng
  email: ecosystem-appeng@redhat.com

