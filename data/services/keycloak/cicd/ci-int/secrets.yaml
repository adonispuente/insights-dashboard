---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: keycloak

name: ci-int-keycloak-secrets

description: ''

app:
  $ref: /services/keycloak/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: secrets

config_path: /jenkins/keycloak/secrets.yaml
