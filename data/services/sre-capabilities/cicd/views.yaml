---
$schema: /dependencies/jenkins-config-1.yml

labels:
  service: sre-capabilities

name: ci-int-sre-capabilities-views

description: ''

app:
  $ref: /services/sre-capabilities/app.yml

instance:
  $ref: /dependencies/ci-int/ci-int.yml

type: views

config:
- view:
    name: sre-capabilities
    description: 'sre-capabilities view'
    view-type: list
    job-filters:
      regex-job:
        regex-name: DESCRIPTION
        regex: '^\[sre-capabilities\].*'
