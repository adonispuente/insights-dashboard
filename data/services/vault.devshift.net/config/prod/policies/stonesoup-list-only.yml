---
$schema: /vault-config/policy-1.yml

labels:
  service: vault.devshift.net

name: "stonesoup-readonly-approle-policy"
instance:
  $ref: /services/vault.devshift.net/config/prod/devshift-net.yml
rules: |
  path "stonesoup/metadata/" {
    capabilities = ["list"]
  }
  path "stonesoup/*" {
    capabilities = ["list"]
  }
