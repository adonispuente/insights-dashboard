$schema: /app-sre/integration-1.yml

labels: {}

name: ldap-groups

description: Manage LDAP groups based on App-Interface roles

upstream: https://github.com/app-sre/qontract-reconcile

pr_check:
  cmd: ldap-groups
  early_exit: true

managed:
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-stage-int.yml
  spec:
    resources:
      requests:
        memory: 300Mi
        cpu: 300m
      limits:
        memory: 300Mi
        cpu: 300m
    logs:
      slack: true
    internalCertificates: true
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-production-int.yml
  spec:
    resources:
      requests:
        memory: 300Mi
        cpu: 300m
      limits:
        memory: 300Mi
        cpu: 300m
    logs:
      slack: true
    internalCertificates: true

schemas:
- /access/role-1.yml
- /access/user-1.yml
- /app-interface/app-interface-settings-1.yml
- /app-sre/integration-1.yml
- /aws/account-1.yml
- /dependencies/dependency-1.yml
