$schema: /app-sre/integration-1.yml

labels: {}

name: aws-cloudwatch-log-retention

description: Add a log retention period on defined cloudwatch log groups.

upstream: https://github.com/app-sre/qontract-reconcile

pr_check:
  cmd: aws-cloudwatch-log-retention

managed:
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-stage-int.yml
  spec:
    resources:
      requests:
        memory: 500Mi
        cpu: 300m
      limits:
        memory: 500Mi
        cpu: 300m
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-production-int.yml
  spec:
    resources:
      requests:
        memory: 500Mi
        cpu: 300m
      limits:
        memory: 500Mi
        cpu: 300m

schemas:
- /aws/account-1.yml
- /app-interface/app-interface-settings-1.yml
- /app-sre/integration-1.yml
- /aws/ecr-1.yml
