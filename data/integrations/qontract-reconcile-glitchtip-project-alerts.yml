$schema: /app-sre/integration-1.yml

labels: {}

name: glitchtip-project-alerts

description: Manages GlitchTip project alerts

upstream: https://github.com/app-sre/qontract-reconcile

pr_check:
  cmd: glitchtip-project-alerts
  early_exit: true

managed:
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-stage-int.appsres05ue1.yml
  spec:
    resources:
      requests:
        memory: 200Mi
        cpu: 25m
      limits:
        memory: 200Mi
    logs:
      slack: true
- namespace:
    $ref: /services/app-interface/namespaces/app-interface-production-int.yml
  spec:
    resources:
      requests:
        memory: 200Mi
        cpu: 25m
      limits:
        memory: 200Mi
    logs:
      slack: true

schemas:
- /app-interface/app-interface-settings-1.yml
- /app-sre/integration-1.yml
- /dependencies/glitchtip-instance-1.yml
- /dependencies/glitchtip-organization-1.yml
- /dependencies/glitchtip-project-1.yml
- /dependencies/glitchtip-project-alert-1.yml
