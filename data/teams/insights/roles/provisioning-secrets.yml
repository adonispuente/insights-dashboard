---
$schema: /access/role-1.yml

labels:
  platform: insights
  service: provisioning

name: provisioning-secrets

permissions:
- $ref: /teams/insights/permissions/crc-provisioning-team-slack.yml

oidc_permissions:
- $ref: /dependencies/vault/permissions/oidc/prod/insights/provisioning.yml
